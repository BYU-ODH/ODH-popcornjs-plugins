/**
 * Stub for link plugin until we work out how we want it to work  
 */
(function (Popcorn) {
   Popcorn.plugin( "link", (function(){
       
       return {
            _setup: function( options ){
            },
            start: function( event, options ){
            },
            end: function( event, options ){
            },
            _tearDown: function( options ){
            }
       }
   })());
})(Popcorn);
